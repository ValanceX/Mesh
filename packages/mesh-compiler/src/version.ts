/**
 * The package's version, which must equal the WebAssembly module's
 * (outline v0.4 I10). `crates/mesh-cli/tests/packages.rs` pins it to
 * `package.json` and to the Rust workspace.
 */
export const version = "0.3.0";
