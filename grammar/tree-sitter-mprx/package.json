{
  "name": "tree-sitter-mprx",
  "version": "1.0.0",
  "description": "Tree-sitter grammar for MPRX (MeshExpr), the declarative UI language used by VALENCE. Consumed by the `mesh-parser` crate to produce a concrete syntax tree, which `mesh-parser` then lowers into the `mesh-syntax` AST.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "tree-sitter-cli": "^0.27.0"
  }
}
